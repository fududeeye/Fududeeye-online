# Fududeeye Online — GitHub / Render Ready

## Run locally
npm install
npm start

Open http://localhost:3000

## Render
Build Command: npm install
Start Command: npm start

Add environment variables from `.env.example` in Render. Never commit a real `.env` file or API keys.

Important: open the app through the server URL, not by opening index.html as a content:// file.
